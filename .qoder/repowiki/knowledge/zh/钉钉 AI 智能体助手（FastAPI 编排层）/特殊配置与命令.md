安装依赖 `pip install -r requirements.txt`；配置 `.env`（复制 `.env.example`）；构建知识库 `python -m rag.ingest [--rebuild]`；启动服务 `uvicorn main:app --host 0.0.0.0 --port 8000 --reload`；REPL 调试 `python main.py --repl`；运行评估 `python -m evaluation.run_eval [-o report.json]`。