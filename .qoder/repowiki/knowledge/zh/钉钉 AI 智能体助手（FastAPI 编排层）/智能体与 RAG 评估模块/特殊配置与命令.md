通过 Python 模块方式运行：`python -m evaluation.run_eval`（全部评估）、`python -m evaluation.run_eval --no-agent`（跳过智能体调用，仅验证评估器）、`python -m evaluation.run_eval --output report.json`（输出 JSON 报告）。需要配置 `llm_api_key`、`llm_base_url`、`llm_judge_model` 以及可选的 `langsmith_api_key`、`langsmith_endpoint`、`langsmith_project`。