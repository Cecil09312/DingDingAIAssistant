# 钉钉AI智能体助手 需求分析文档

> 版本：2.0
> 日期：2026-08-09  
> 状态：已实现

---

## 1. 项目概述

### 1.1 项目背景

钉钉作为企业级协同办公平台，需一个能接入钉钉消息通道、具备智能问答能力的 AI 智能体助手，为钉钉用户提供智能化、个性化的对话与知识服务。该助手需融合检索增强生成（RAG）、记忆持久化与多维度评估能力，构建一个完整、可评测的企业 AI 智能体系统。

### 1.2 项目目标

| 目标 | 描述 |
|------|------|
| 智能问答 | 基于 RAG 实现知识库驱动的准确问答，支持多轮对话上下文连贯 |
| 记忆持久化 | 短期记忆维护会话上下文，长期记忆实现跨会话个性化 |
| 质量评估 | 对 RAG 检索质量与智能体回答质量进行自动化、系统化评测 |
| 钉钉集成 | 对接钉钉 Webhook 回调与主动消息发送，完成闭环交互 |
| Web 聊天界面 | 提供浏览器前端聊天页面，支持多轮对话与会话重置，便于直接调试体验 |

### 1.3 用户角色

| 角色 | 说明 |
|------|------|
| 钉钉终端用户 | 通过钉钉群/单聊与智能体交互的普通员工 |
| 系统管理员 | 配置钉钉应用凭证、管理知识文档、运行评估的运维人员 |
| 评估人员 | 使用评估 CLI 对智能体回答质量进行评测的开发/测试人员 |

---

## 2. 功能需求

### 2.1 智能问答与 RAG 系统（FR-RAG）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-R1 | 实现基于 RAG（检索增强生成）的智能问答完整 pipeline | 高 | 已实现 |
| FR-R2 | 文档加载：支持 .txt / .md / .pdf / .docx / .xlsx 及图片(.png/.jpg/.jpeg/.bmp/.tiff) | 高 | 已实现 |
| FR-R3 | 文本切分：RecursiveCharacterTextSplitter，chunk_size / chunk_overlap 可配置（默认 500 / 80，环境变量 RAG_CHUNK_SIZE / RAG_CHUNK_OVERLAP） | 高 | 已实现 |
| FR-R4 | 向量化存储：ChromaDB 持久化（文本+图像统一 collection） | 高 | 已实现 |
| FR-R5 | Embedding 模型：BAAI/bge-small-zh-v1.5（纯文本向量，512 维，中文优化，编码速度 0.2s/次） | 高 | 已实现 |
| FR-R6 | 语义检索：similarity_search_with_score，带相关度过滤，支持文本+图像混合检索 | 高 | 已实现 |
| FR-R7 | 生成式回答：融合 RAG 上下文 + 长期记忆生成回答 | 高 | 已实现 |
| FR-R8 | 路由判断：小模型(qwen-turbo)意图识别 + 关键词兜底，区分知识问答与闲聊；RAG 优先：可入库回答的问题优先走知识库检索，仅时效性问题走联网，不确定时选 RAG | 高 | 已实现 |
| FR-R9 | 文档入库 CLI（支持 --rebuild 重建索引） | 中 | 已实现 |
| FR-R10 | PDF 处理：PyPDFLoader 文本提取 + 扫描件 OCR 兜底（easyocr） | 高 | 已实现 |
| FR-R11 | Word 处理：Docx2txtLoader 提取 .docx 文本 | 高 | 已实现 |
| FR-R12 | Excel 处理：openpyxl 遍历所有 sheet 单元格 | 高 | 已实现 |
| FR-R13 | OCR 处理：easyocr 对图片/扫描页做文字识别（支持 ch_sim+en） | 高 | 已实现 |
| FR-R14 | 图像入库：图片文档通过 embed_images 编码存入向量库，支持跨模态检索 | 中 | 已实现 |
| FR-R15 | 两阶段检索：向量检索召回 top-N 候选 + CrossEncoder 重排序精排 top-k | 高 | 已实现 |
| FR-R16 | 重排序模型：BAAI/bge-reranker-base（中文优化，CrossEncoder） | 高 | 已实现 |
| FR-R17 | 重排序开关：可通过 RERANK_ENABLED 配置启用/禁用，禁用时回退直接向量检索 | 中 | 已实现 |
| FR-R18 | 重排序容错：重排序失败时回退返回原始向量检索结果 | 中 | 已实现 |
| FR-R19 | 统一路由：小模型意图识别输出 rag(知识库)/web(联网搜索)/chat(闲聊) + need_memory(是否加载长期记忆)，条件边合并为 unified_route_condition | 高 | 已实现 |
| FR-R20 | 联网搜索：DuckDuckGo（主引擎）+ 百度（备用引擎）双引擎搜索 | 高 | 已实现 |
| FR-R21 | 搜索结果格式化：搜索结果含标题、URL、摘要，格式化为上下文供生成节点使用 | 高 | 已实现 |
| FR-R22 | 搜索开关：可通过 WEB_SEARCH_ENABLED 配置启用/禁用，禁用时降级为两路 rag/chat | 中 | 已实现 |

**RAG pipeline 流程：**

```
文档加载(txt/md/pdf/docx/xlsx/图片) → OCR(扫描件) → 文本切分 → 多模态 Embedding 向量化 → ChromaDB 存储
                                                                                                ↓
用户提问 → 统一路由(小模型意图识别, rag/web/chat + need_memory)
  ├─ rag  → 向量检索(top-20候选) → 重排序(CrossEncoder精排) → 取top-4 → 上下文格式化
  ├─ web  → 联网搜索(DuckDuckGo/百度) → 结果格式化
  └─ chat → 直接生成
→ 生成回答
```

### 2.3 RAG 质量评估（FR-RAG-EVAL）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-RE1 | 检索相关性评估（retrieval_relevance）：检索文档与问题的相关程度 | 高 | 已实现 |
| FR-RE2 | 答案忠实度评估（groundedness）：答案是否基于检索上下文，无幻觉 | 高 | 已实现 |
| FR-RE3 | 回答帮助度评估（helpfulness）：回答对用户的实际帮助价值 | 中 | 已实现 |
| FR-RE4 | 答案相关性评估（answer_relevance）：回答是否切题 | 中 | 已实现 |
| FR-RE5 | 使用 OpenEvals create_llm_as_judge + 预置 RAG prompt 实现自动化评测 | 高 | 已实现 |
| FR-RE6 | 每个评估维度输出 score + reasoning | 高 | 已实现 |

### 2.4 记忆系统（FR-MEMORY）

#### 2.4.1 短期记忆

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-SM1 | 维护当前会话的上下文与交互历史 | 高 | 已实现 |
| FR-SM2 | 支持多轮对话的上下文连贯性 | 高 | 已实现 |
| FR-SM3 | 基于 LangGraph checkpointer，按 thread_id（=session_id）管理 | 高 | 已实现 |
| FR-SM4 | 使用进程内 MemorySaver 作为 checkpointer | 高 | 已实现 |
| FR-SM5 | 短期历史窗口/字符双预算（默认窗口 8 条、预算 2000 字符），超预算从最旧消息丢弃 | 高 | 已实现 |
| FR-SM6 | 超窗时注入会话压缩摘要（session_summary 表，SystemMessage【更早对话摘要】），滑动不丢主线 | 高 | 已实现 |

#### 2.4.2 长期记忆

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-LM1 | 使用 SQLite 作为持久化存储后端（Python 内置，无需外部服务） | 高 | 已实现 |
| FR-LM2 | 保存跨会话的用户偏好（user_prefs 表，键值对） | 高 | 已实现 |
| FR-LM3 | 保存关键信息摘要及历史交互摘要（LLM 摘要 + 截断兜底） | 高 | 已实现 |
| FR-LM4 | 每 N 轮对话自动触发摘要更新（默认 6 轮） | 中 | 已实现 |
| FR-LM5 | 摘要历史列表保留最近 20 条（summary_history 表） | 低 | 已实现 |
| FR-LM6 | 加载长期记忆上下文供生成节点使用，实现个性化服务 | 高 | 已实现 |
| FR-LM7 | 结构化记忆：用户画像（user_profile）+ 关键事实（user_facts，带优先级 1-10）+ 会话压缩摘要（session_summary） | 高 | 已实现 |
| FR-LM8 | 关键信息抽取：每轮规则快速抽取（姓名/项目等，零 LLM 成本）+ 可选 LLM 结构化抽取（JSON 输出） | 高 | 已实现 |
| FR-LM9 | 优先级分层加载与预算控制：P0 画像/关键事实硬保留，P1 关键词召回+最近摘要，P2 旧摘要按剩余预算填充（默认 1600 字符） | 高 | 已实现 |
| FR-LM10 | 摘要增量合并：携带旧摘要生成新摘要，强制保留关键事实，key_facts 落库 user_facts | 中 | 已实现 |
| FR-LM11 | 分节注入模板（用户画像/已知事实/历史摘要）+ 显式使用指令，RAG 上下文按预算截断（默认 2500 字符） | 高 | 已实现 |
| FR-LM12 | 前端 localStorage 持久化随机 user_id，避免多用户记忆互相污染 | 高 | 已实现 |
| FR-LM13 | 问答记忆缓存：历史问答对带向量落库（qa_memory），新问题余弦相似度达阈（默认 0.90）时直接复用历史答案，跳过大模型生成；仅缓存 rag/web 路由的知识类回答，每用户上限 200 条 | 高 | 已实现 |

**SQLite 表结构设计：**

```sql
-- 用户记忆主表（摘要 + 轮次计数）
CREATE TABLE user_memory (
    user_id    TEXT PRIMARY KEY,
    summary    TEXT DEFAULT '',
    updated_at INTEGER DEFAULT 0,
    turn_count INTEGER DEFAULT 0
);

-- 用户偏好表（键值对）
CREATE TABLE user_prefs (
    user_id TEXT NOT NULL,
    key     TEXT NOT NULL,
    value   TEXT DEFAULT '',
    PRIMARY KEY (user_id, key)
);

-- 摘要历史表（自动递增，按时间倒序保留最近 20 条）
CREATE TABLE summary_history (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id    TEXT NOT NULL,
    summary    TEXT NOT NULL,
    created_at INTEGER NOT NULL
);
CREATE INDEX idx_history_user ON summary_history(user_id, created_at DESC);

-- 用户画像（单值可更新，最高优先级）
CREATE TABLE user_profile (
    user_id TEXT, field TEXT,          -- name/nickname/role/project/company
    value TEXT, updated_at INTEGER,
    PRIMARY KEY (user_id, field)
);

-- 关键事实（多条，带优先级与来源）
CREATE TABLE user_facts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT, fact TEXT,
    priority INTEGER DEFAULT 5,        -- 1(最高)~10；"请记住/我叫"类=1，摘要提炼=3
    source TEXT, created_at INTEGER, updated_at INTEGER
);

-- 会话压缩摘要（短期记忆超窗后的滚动摘要，按 session）
CREATE TABLE session_summary (
    session_id TEXT PRIMARY KEY, user_id TEXT,
    summary TEXT, covered_until INTEGER, updated_at INTEGER
);

-- 问答记忆缓存（相似问题直接复用历史答案）
CREATE TABLE qa_memory (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT, question TEXT, answer TEXT,
    embedding BLOB,                    -- 问题向量（float32）
    hit_count INTEGER DEFAULT 0, created_at INTEGER, updated_at INTEGER
);
```

SQLite 配置：WAL 模式（并发读不阻塞）+ busy_timeout=5000ms + threading.Lock 写串行化。

### 2.5 智能体评估（FR-AGENT-EVAL）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-AE1 | 使用 LangSmith 和/或 OpenEvals 构建评估体系 | 高 | 已实现 |
| FR-AE2 | 评估维度：回答质量（correctness 正确性） | 高 | 已实现 |
| FR-AE3 | 评估维度：幻觉检测（hallucination） | 高 | 已实现 |
| FR-AE4 | 评估维度：推理路径合理性（plan_adherence） | 高 | 已实现 |
| FR-AE5 | LangSmith 客户端拉取 trace 做路径合理性分析（成功率统计） | 中 | 已实现 |
| FR-AE6 | 二者结合使用以互补评估能力 | 高 | 已实现 |
| FR-AE7 | 评估运行 CLI，输出 JSON 报告 + 控制台汇总表格 | 高 | 已实现 |

### 2.6 钉钉集成（FR-DINGTALK）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-D1 | 提供 Webhook 接收钉钉消息回调（由 Skill 独立服务 webhook_server.py 提供，不侵入项目源码） | 高 | 已实现 |
| FR-D2 | HMAC-SHA256 签名校验，确保请求来源合法 | 高 | 已实现 |
| FR-D3 | 签名校验含时间戳防重放（默认 1 小时有效期） | 高 | 已实现 |
| FR-D4 | 接收钉钉消息后调用智能体生成回答 | 高 | 已实现 |
| FR-D5 | 通过钉钉消息接口返回智能回复 | 高 | 已实现 |
| FR-D6 | 获取企业 access_token（带本地缓存过期） | 高 | 已实现 |
| FR-D7 | 支持工作通知（文本/Markdown）与机器人 webhook 回复 | 中 | 已实现 |
| FR-D8 | 钉钉功能完全以 Agent Skill 形式提供，项目源码不含钉钉代码（dingtalk/ 包已移除） | 高 | 已实现 |
| FR-D9 | Skill 自包含公共库 dingtalk_lib.py：发送函数含输入校验与错误处理，失败返回错误码不抛异常 | 高 | 已实现 |
| FR-D10 | 凭证从环境变量读取（自动加载项目 .env），签名校验自动读取 DINGTALK_ROBOT_SECRET | 中 | 已实现 |
| FR-D11 | 项目源码与 Skill 解耦：主服务仅提供 Web 聊天，钉钉能力可独立启用/停用 | 中 | 已实现 |
| FR-D12 | Agent Skill：.qoder/skills/dingtalk-messaging/SKILL.md（项目级，随仓库分发），指导连接钉钉、收发消息与 Webhook 处理 | 高 | 已实现 |
| FR-D13 | Skill 脚本：send_message.py（发送测试消息）+ test_webhook.py（模拟回调）+ webhook_server.py（回调接收服务），存放于 scripts/ | 中 | 已实现 |

### 2.6.1 钉钉工具调用（FR-TOOL）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-T1 | 工具调用总开关：TOOL_CALLING_ENABLED 控制是否启用工具路由（关闭时自动降级为 chat） | 高 | 已实现 |
| FR-T2 | 路由判断增加 tool 选项：关键词短路 + LLM 路由 + 关键词兜底三层判断，工具优先 | 高 | 已实现 |
| FR-T3 | 待办管理：创建待办（create_todo），支持标题/截止时间/详情/优先级 | 高 | 已实现 |
| FR-T4 | 待办管理：查询待办（query_todos），支持按状态过滤 | 高 | 已实现 |
| FR-T5 | 会议管理：创建会议（create_meeting），支持主题/时间/参会人/地点 | 高 | 已实现 |
| FR-T6 | 会议管理：取消会议（cancel_meeting），支持按标题匹配或会议ID取消 | 高 | 已实现 |
| FR-T7 | 会议管理：修改会议（update_meeting），支持按标题匹配或会议ID修改 | 高 | 已实现 |
| FR-T8 | 会议管理：查询会议（query_meetings），支持日期范围过滤 | 高 | 已实现 |
| FR-T9 | LLM Function Calling 参数提取：用路由小模型从自然语言提取工具名+参数 | 高 | 已实现 |
| FR-T10 | 写操作确认机制：创建/取消/修改操作需用户回复「确认」后执行（TOOL_CONFIRMATION_REQUIRED） | 高 | 已实现 |
| FR-T11 | 自然语言时间解析：支持「明天下午3点」「下周一上午10点」「本周」等中文时间表达 | 高 | 已实现 |
| FR-T12 | 参会人解析：姓名/手机号 → 钉钉用户ID（通过通讯录 API 查询） | 中 | 已实现 |
| FR-T13 | 工具执行兜底：API 调用失败返回友好错误信息，不抛异常 | 高 | 已实现 |
| FR-T14 | 钉钉 API 客户端扩展：dingtalk_lib.py 新增通用 _post_api 封装 + 待办3方法 + 会议4方法 + 通讯录2方法 | 高 | 已实现 |
| FR-T15 | 工具模块独立：agent/tools/ 目录包含 tool_schemas/todo_tools/meeting_tools/time_parser | 高 | 已实现 |

### 2.7 LangGraph 工作流编排（FR-WORKFLOW）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-W1 | 使用 LangGraph StateGraph 实现智能体状态图工作流 | 高 | 已实现 |
| FR-W2 | 支持多步骤推理与决策路由（条件边） | 高 | 已实现 |
| FR-W3 | 统一路由节点(小模型意图识别) → 加载长期记忆节点(按需，闲聊跳过) → 问答记忆匹配节点（命中短路到结束） | 高 | 已实现 |
| FR-W4 | 统一条件分流：unified_route_condition 合并问答记忆命中判断与三路路由(rag/web/chat)，命中→END，否则按 search_route 分流 | 高 | 已实现 |
| FR-W5 | 生成回答节点 → 关键信息抽取节点（规则 + 可选 LLM）→ 更新长期记忆节点 → 结束 | 高 | 已实现 |
| FR-W6 | 编译时挂载 checkpointer，支持多轮对话状态管理 | 高 | 已实现 |

**工作流状态图（统一路由）：**

```
START → pre_check(qa_match 问答缓存匹配 + route 小模型意图识别, 输出 search_route)
                        ↓
            pre_check_condition 五路分流
                ├── memory_hit → END（复用历史答案）
                ├── tool  → tool（待办/会议操作）→ END
                ├── rag   → retrieve → generate
                ├── web   → web_search → generate
                └── chat  → load_memory → generate
                                    ↓
                          memory_background（抽取+记忆更新，后台执行）
                                    ↓
                                   END
```

---

## 3. 非功能需求

### 3.1 性能需求（NFR-PERF）

| 编号 | 需求项 | 指标 |
|------|--------|------|
| NFR-P1 | 单轮问答响应时间 | LLM 调用 < 10s，含检索 < 15s |
| NFR-P2 | RAG 检索延迟 | 本地 Embedding + ChromaDB < 500ms |
| NFR-P3 | 长期记忆读写延迟 | SQLite 读写 < 10ms（WAL 模式） |
| NFR-P4 | access_token 缓存命中率 | > 99%（本地缓存避免重复请求） |
| NFR-P5 | 辅助任务使用小模型 | 路由/抽取用 qwen-turbo，节省 ~15-20s |
| NFR-P6 | 路由三层判断 | 规则短路 → LLM 意图识别 → 关键词兜底，兼顾速度与准确率 |
| NFR-P7 | RAG 向量复用 | qa_match 产出的 query_embedding 供 retrieve 复用，避免重复编码 |
| NFR-P8 | BGE Embedding 编码速度 | 0.2s/次（替代 Jina CLIP v2 的 6-7s/次，30x 加速） |
| NFR-P9 | 工具调用响应时间 | 参数提取 < 3s，API 调用 < 5s |

### 3.2 可靠性需求（NFR-REL）

| 编号 | 需求项 | 说明 |
|------|--------|------|
| NFR-R1 | LLM 调用失败降级 | 路由→关键词兜底；摘要→截断兜底；生成→多模型优先级降级（LLM_FALLBACK_MODELS） |
| NFR-R2 | 外部依赖失败降级 | 短期记忆使用进程内 MemorySaver（无外部依赖）；长期记忆使用 SQLite |
| NFR-R3 | 签名校验失败拒绝 | 返回 403，不处理非法请求 |
| NFR-R4 | 向量库检索失败容错 | 捕获异常返回空上下文，不阻断生成流程 |
| NFR-R5 | LLM 重试与超时 | max_retries=3 指数退避重试，request_timeout=60s 超时保护 |
| NFR-R6 | 流式接口超时保护 | asyncio.wait_for 整体超时，超时后返回已生成内容 |
| NFR-R7 | 检索相关度阈值过滤 | rag_min_relevance=0.3，低于阈值的文档不注入上下文 |
| NFR-R8 | 多路召回容错 | BM25 失败时降级为纯向量检索，rank-bm25 未安装时静默跳过 |
| NFR-R9 | 查询改写容错 | 改写失败时回退原始查询，不阻断检索 |
| NFR-R10 | 输入安全过滤 | 关键词黑名单 + prompt 注入检测，默认关闭 |
| NFR-R11 | 限流与熔断 | 滑动窗口限流 + 三态熔断器（closed/open/half_open），默认关闭 |
| NFR-R12 | 工具调用兜底 | API 失败返回友好错误，工具路由关闭时降级为 chat |
| NFR-R13 | 工具写操作确认 | 创建/取消/修改操作需用户回复「确认」后执行 |

### 3.3 安全需求（NFR-SEC）

| 编号 | 需求项 | 说明 |
|------|--------|------|
| NFR-S1 | 钉钉回调签名校验 | HMAC-SHA256 + compare_digest 防时序攻击 |
| NFR-S2 | 凭证隔离 | 所有密钥/API Key 通过 .env 配置，不硬编码 |
| NFR-S3 | 时间戳防重放 | 签名有效期限制 1 小时 |

### 3.4 可维护性需求（NFR-MAINT）

| 编号 | 需求项 | 说明 |
|------|--------|------|
| NFR-M1 | 模块化设计 | 各功能模块独立，职责单一，可独立修改测试 |
| NFR-M2 | 配置外置 | pydantic-settings 统一管理，.env 驱动 |
| NFR-M3 | 冒烟测试 | 19 项冒烟测试覆盖核心模块导入与基础功能（含多模态+重排序+联网搜索+钉钉 Skill 模块） |
| NFR-M4 | 文档完备 | README + 需求分析文档 + 代码注释 |

---

## 4. 技术需求

### 4.1 核心技术栈

| 类别 | 技术选型 | 版本要求 | 用途 |
|------|----------|----------|------|
| 框架 | LangChain | >=1.2.4 | 应用逻辑与 LLM 调用链路 |
| 编排 | LangGraph | >=1.0.6 | StateGraph 工作流编排 |
| LLM | 千问（OpenAI 兼容） | - | 生成与推理模型 |
| Embedding | BAAI/bge-small-zh-v1.5 | sentence-transformers>=2.7.0 | 纯文本 Embedding（512 维，中文优化，0.2s/次） |
| BM25 检索 | rank-bm25 | >=0.2.2 | 关键词检索（多路召回，默认关闭） |
| 向量库 | ChromaDB | >=0.5.0 | 语义检索存储 |
| 记忆后端（短期） | langgraph-checkpoint（MemorySaver） | - | 短期记忆 checkpointer（进程内） |
| 记忆后端（长期） | SQLite（Python 内置 sqlite3） | - | 长期记忆持久化 |
| 评估 | OpenEvals + LangSmith | openevals>=0.1.3 | 质量评估与 trace 分析 |
| Web 服务 | FastAPI + uvicorn | fastapi>=0.115 | API 服务与 Web 聊天界面 |
| 前端 | 原生 HTML/CSS/JS | - | 单页聊天界面（无需构建工具） |

### 4.2 运行环境

| 项目 | 要求 |
|------|------|
| Python 版本 | 3.12+ |
| 操作系统 | Windows / Linux / macOS |
| SQLite | data/memory.db（长期记忆持久化，Python 内置无需安装） |
| 短期记忆 | 进程内 MemorySaver，无需外部服务 |
| 网络环境 | 需访问千问 API；HuggingFace 模型下载配置 hf-mirror.com 镜像 |

### 4.3 配置项清单

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| LLM_MODEL | qwen-plus | 千问模型名 |
| LLM_BASE_URL | https://dashscope.aliyuncs.com/compatible-mode/v1 | OpenAI 兼容端点 |
| LLM_API_KEY | （需填入） | 千问 API Key |
| LLM_JUDGE_MODEL | qwen-plus | 评估评判模型 |
| EMBEDDING_MODEL | BAAI/bge-small-zh-v1.5 | 纯文本 Embedding 模型（512 维，中文优化） |
| EMBEDDING_DEVICE | cpu | Embedding 运行设备 |
| OCR_LANGUAGES | ch_sim,en | easyocr 语言（逗号分隔） |
| OCR_USE_GPU | false | OCR 是否使用 GPU |
| OCR_MIN_TEXT_LENGTH | 50 | PDF 页面文本少于此字数触发 OCR |
| SUPPORTED_FILE_EXTENSIONS | .txt,.md,.pdf,.docx,.xlsx,.png,.jpg,.jpeg,.bmp,.tiff | 支持的文件扩展名 |
| RERANK_ENABLED | true | 是否启用 CrossEncoder 重排序 |
| RERANK_MODEL | BAAI/bge-reranker-base | 重排序模型名 |
| RERANK_DEVICE | cpu | 重排序运行设备 |
| RERANK_CANDIDATE_COUNT | 20 | 向量检索召回的候选数 |
| RERANK_TOP_K | 4 | 重排序后返回的 top-k |
| WEB_SEARCH_ENABLED | true | 是否启用联网搜索 |
| WEB_SEARCH_MAX_RESULTS | 5 | 联网搜索最大结果数 |
| WEB_SEARCH_TIMEOUT | 10 | 搜索超时（秒） |
| CHROMA_PERSIST_DIR | data/chroma | 向量库持久化路径 |
| LONG_TERM_DB_PATH | data/memory.db | 长期记忆 SQLite 数据库路径 |
| DINGTALK_APP_KEY | （需填入） | 钉钉企业应用 AppKey（仅供 dingtalk-messaging Skill 使用） |
| DINGTALK_APP_SECRET | （需填入） | 钉钉企业应用 AppSecret（仅供 dingtalk-messaging Skill 使用） |
| DINGTALK_ROBOT_SECRET | （需填入） | 机器人加签密钥（仅供 dingtalk-messaging Skill 使用） |
| LANGSMITH_API_KEY | （需填入） | LangSmith API Key |
| MEMORY_SUMMARY_EVERY | 6 | 每 N 轮触发长期记忆摘要 |
| LLM_ROUTER_MODEL | qwen-turbo | 路由小模型（路由/抽取） |
| LLM_MAX_RETRIES | 3 | LLM 最大重试次数 |
| LLM_REQUEST_TIMEOUT | 60 | LLM 请求超时（秒） |
| LLM_FALLBACK_MODELS | qwen-turbo,qwen-plus | 备用模型优先级列表 |
| STREAM_TIMEOUT | 60 | 流式接口整体超时（秒） |
| RAG_MIN_RELEVANCE | 0.3 | 检索最低相关度（0~1） |
| RAG_BM25_ENABLED | false | BM25 多路召回开关 |
| RAG_QUERY_REWRITE_ENABLED | false | 查询改写开关 |
| INPUT_FILTER_ENABLED | false | 输入安全过滤开关 |
| RATE_LIMIT_PER_MINUTE | 0 | 每用户每分钟请求上限（<=0 不限流） |
| CIRCUIT_BREAKER_THRESHOLD | 0 | 熔断阈值（<=0 不熔断） |
| TOOL_CALLING_ENABLED | false | 钉钉工具调用总开关 |
| TOOL_CONFIRMATION_REQUIRED | true | 工具写操作需用户确认 |

---

### 2.8 Web 聊天界面（FR-WEB-UI）

| 编号 | 需求项 | 优先级 | 实现状态 |
|------|--------|--------|----------|
| FR-WU1 | 提供 Web 浏览器聊天界面（HTML 单页应用），访问根路径 `/` 加载 | 高 | 已实现 |
| FR-WU2 | 聊天界面支持消息输入与发送，Enter 发送、Shift+Enter 换行 | 高 | 已实现 |
| FR-WU3 | 显示用户消息与助手回复气泡，区分左右侧布局 | 高 | 已实现 |
| FR-WU4 | 助手思考中显示打字动画指示器 | 中 | 已实现 |
| FR-WU5 | 支持「重置会话」按钮，生成新的 session_id 开启新对话 | 高 | 已实现 |
| FR-WU6 | 前端通过 POST `/api/chat` 调用智能体，复用 LangGraph 工作流与记忆系统 | 高 | 已实现 |
| FR-WU7 | 界面状态栏显示服务连接状态（就绪/思考中/错误） | 中 | 已实现 |
| FR-WU8 | 聊天页面响应式布局，适配桌面与移动端 | 低 | 已实现 |

**Web 聊天架构：**

```
浏览器 (static/index.html)
    ↓ POST /api/chat {user_input, user_id, session_id}
FastAPI → LangGraph 智能体（路由→RAG/生成→记忆）
    ↓ {errcode, errmsg, answer}
浏览器渲染助手回复气泡
```

---

## 5. 接口需求

### 5.1 HTTP API

| 方法 | 路径 | 请求 | 响应 | 说明 |
|------|------|------|------|------|
| GET | / | - | HTML 聊天页面 | Web 聊天前端界面 |
| GET | /health | - | `{status, service, checkpointer}` | 健康检查 |
| POST | /api/chat | `{user_input, user_id, session_id}` | `{errcode, errmsg, answer}` | Web 端智能体聊天 |

> 钉钉回调端点 `POST /dingtalk/webhook` 已迁移至 Skill 独立服务（`.qoder/skills/dingtalk-messaging/scripts/webhook_server.py`），不在主服务中提供。

### 5.2 钉钉接口对接（由 Skill 调用）

| 接口 | 方法 | 用途 |
|------|------|------|
| /gettoken | GET | 获取企业 access_token |
| /topapi/message/corpconversation/asyncsend_v2 | POST | 工作通知消息发送 |
| /robot/send | POST | 机器人 webhook 群消息回复 |

### 5.3 CLI 工具

| 命令 | 用途 |
|------|------|
| `python -m rag.ingest [--rebuild]` | 文档入库/重建索引 |
| `python -m evaluation.run_eval [--no-agent] [-o file]` | 运行评估 |
| `python main.py --repl` | 本地 REPL 调试 |

---

## 6. 评估体系详细设计

### 6.1 RAG 质量评估维度（OpenEvals）

| 维度 | Prompt 来源 | 评估对象 | 评分含义 |
|------|-------------|----------|----------|
| 检索相关性 | RAG_RETRIEVAL_RELEVANCE_PROMPT | question vs retrieved_docs | 检索文档与问题相关度 |
| 忠实度 | RAG_GROUNDEDNESS_PROMPT | answer vs context | 答案基于上下文无幻觉 |
| 帮助度 | RAG_HELPFULNESS_PROMPT | answer vs question | 回答的实际帮助价值 |
| 答案相关性 | ANSWER_RELEVANCE_PROMPT | answer vs question | 回答切题程度 |

### 6.2 智能体评估维度（OpenEvals + LangSmith）

| 维度 | Prompt 来源 | 评估对象 | 评分含义 |
|------|-------------|----------|----------|
| 正确性 | CORRECTNESS_PROMPT | answer vs expected_answer | 回答与参考答案一致性 |
| 幻觉检测 | HALLUCINATION_PROMPT | answer vs context | 是否存在编造内容 |
| 推理路径 | PLAN_ADHERENCE_PROMPT | question vs answer | 是否按预期步骤推进 |
| LangSmith trace | - | 运行记录 | 成功率与路径合理性统计 |

### 6.3 评估数据集

内置 5 个评测样本，覆盖产品功能、技术栈、记忆系统、评估维度等核心知识点，每个样本包含：
- `question`：用户问题
- `expected_answer`：期望答案
- `reference_context`：参考资料文本
- `reference_docs`：检索文档片段列表

### 6.4 评估报告输出

- JSON 报告文件（含每个样本的逐维度评分与 reasoning）
- 控制台汇总表格（各维度平均分 + LangSmith 路径分析统计）

---

## 7. 数据需求

### 7.1 知识文档

- 存放路径：可配置（默认 `data/docs/`，环境变量 `RAG_DOCS_DIR`，支持绝对路径）
- 支持格式：.txt / .md / .pdf / .docx / .xlsx / .png / .jpg / .jpeg / .bmp / .tiff
- 示例文档：产品说明.txt（钉钉AI智能体助手产品功能与架构说明）
- 入库工具：`python -m rag.ingest`

### 7.2 向量库数据

- 持久化路径：`data/chroma/`
- Collection 名称：dingtalk_kb
- 切分参数：chunk_size / chunk_overlap 可配置（默认 500 / 80，调整后需 --rebuild 重建索引）

### 7.3 记忆数据

- 短期记忆：LangGraph checkpointer（进程内 MemorySaver）
- 长期记忆：SQLite 表结构（user_memory / user_prefs / summary_history，见 2.4.2）

### 7.4 评估数据

- 评估数据集：`evaluation/datasets.py`（内置 5 样本）
- 评估报告：JSON 格式（可指定输出路径）

---

## 8. 约束与假设

### 8.1 约束

| 编号 | 约束 |
|------|------|
| C1 | LLM 必须通过 OpenAI 兼容接口调用（千问 DashScope 兼容模式） |
| C2 | Embedding 必须本地运行，不依赖外部 API |
| C3 | 长期记忆必须使用 SQLite 作为持久化后端（Python 内置，无需外部服务） |
| C4 | 评估必须同时支持 OpenEvals 和 LangSmith |
| C5 | 钉钉回调必须进行签名校验（由 dingtalk-messaging Skill 实现） |

### 8.2 假设

| 编号 | 假设 |
|------|------|
| A1 | 用户已在 .env 中填入千问 LLM_API_KEY / LLM_BASE_URL / LLM_MODEL |
| A2 | 用户已填入钉钉应用凭证（AppKey/AppSecret/Robot Secret） |
| A3 | 短期记忆使用进程内 MemorySaver，无需 Redis 等外部服务 |
| A4 | HuggingFace 模型可通过 hf-mirror.com 镜像下载 |
| A5 | LangSmith API Key 已配置（未配置时评估模块自动跳过 LangSmith 部分） |

---

## 9. 验收标准

| 编号 | 验收项 | 验收方法 | 状态 |
|------|--------|----------|------|
| V1 | 依赖安装成功 | `pip install -r requirements.txt` | 通过 |
| V2 | 关键模块导入无错 | `from agent.graph import build_graph` 等 | 通过 |
| V3 | 冒烟测试全通过 | `python tests/test_smoke.py`（19/19） | 通过 |
| V4 | RAG 文档入库成功 | `python -m rag.ingest` 入库 chunk 数 > 0 | 通过 |
| V5 | RAG 检索返回相关结果 | `retrieve_context()` 返回含来源与相关度的上下文 | 通过 |
| V6 | FastAPI 启动正常 | `from main import app` 加载成功，含 /health 路由 | 通过 |
| V7 | 长期记忆 SQLite 存取正常 | 冒烟测试 test_long_term_memory 通过 | 通过 |
| V8 | 签名校验正确 | 合法签名通过、非法签名拒绝 | 通过 |

---

## 10. 需求追溯矩阵

| 原始需求 | 对应功能需求 | 实现模块 | 验收项 |
|----------|-------------|----------|--------|
| LangChain+LangGraph | FR-WORKFLOW | agent/graph.py, nodes.py | V2, V3 |
| 智能问答+多模态RAG+联网搜索 | FR-RAG | rag/*（含 ocr.py, reranker.py, web_search.py） | V4, V5 |
| RAG质量评估 | FR-RAG-EVAL | evaluation/rag_eval.py | V3 |
| 短期记忆 | FR-SM1~4 | memory/short_term.py | V3 |
| 长期记忆(SQLite) | FR-LM1~6 | memory/long_term.py | V3, V7 |
| 智能体评估 | FR-AGENT-EVAL | evaluation/agent_eval.py | V3 |
| LangSmith/OpenEvals | FR-AE1~7 | evaluation/agent_eval.py, rag_eval.py | V3 |
| 钉钉集成 | FR-DINGTALK | .qoder/skills/dingtalk-messaging（自包含 Skill，项目源码无钉钉代码） | V6, V8 |
| 项目结构 | - | 全部模块 + requirements.txt + README | V1 |
